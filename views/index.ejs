<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/public/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
</head>
<body>
<div class="parent-container">

    <div class="search-container">
        <fieldset>
            <form action="/" method="post">
                <input name="food" type="text" class="ghost-input" placeholder="Enter a Food" required>
                <input type="submit" class="ghost-button" value="Get Recipie">
            </form>
        </fieldset>
    </div>

    <div class="results-container">
        <% if(recipie !== null) { %>
            <p><%= recipie %></p>
        <% } %>

        <% if(error !== null){ %>
            <p><%= error %></p>
        <% } %>
    </div>
</div>
</body>
</html>